import 'package:flutter/material.dart';

class ProductUnitsPrices extends StatelessWidget {
  const ProductUnitsPrices({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
